#include "../packets.h"

bool8
make_packet(enum PACKET_TYPE type, void *packet)
{
  switch (type)
  {
    case ACK:
      {

      } break;
    case LOGIN:
      {

      } break;

    case HEARTBEAT:
      {

      } break;
  }
}
